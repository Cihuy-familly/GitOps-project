apiVersion: v1
kind: Service
metadata:
  name: cihuylabs-landing
  namespace: cihuylabs-landing
spec:
  type: ClusterIP
  selector:
    app: cihuylabs-landing
  ports:
    - port: 80
      targetPort: 80
      name: http
